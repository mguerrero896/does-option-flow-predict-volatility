# Results superseded by RP2-v3

A result that RP2-v3 replaces is marked `SUPERSEDED_BY_RP2_V3` and is **never deleted**.
The frozen artifact, its hash and its provenance stay exactly where they were: a reader
who followed a citation to an RP2-v2 number must still find that number, and must also
find the record that a later run replaced it and why.

## Marker

```text
SUPERSEDED_BY_RP2_V3
```

Carried in three places, so that no single omission hides the supersession:

1. the superseding run's `scorecard.json`, under the artifact it replaces;
2. `public.rp2_block_results.supersedes_run_id`, with `is_current` moved to the new row;
3. the table below.

## Register

| Superseded artifact | Superseded by | Reason | Recorded |
| --- | --- | --- | --- |
| `artifacts/rp2_block4_b0/ladder.json` → `results.{D,V}.ewma` | `fix/rp2-v3-causal-b0` | the EWMA challenger was built from the square root of the RV30 target rather than from observed one-minute returns | 2026-08-20 |
| `artifacts/rp2_block5_surface/surface_coverage.json` and the B1 panel | `feat/rp2-v3-contemporaneous-b1` | the snapshot ended 1 920 s before the origin, and the primary set carried two 60 %-coverage diagnostics | 2026-08-20 |
| `artifacts/rp2_block6_flow/flow_coverage.json` (`d7320a54…`, 60 features) and the B2 panel | `artifacts/rp2_v3/gate5-exact-clock-b2/flow_coverage.json` (`773c3d3d…`, 70 features) | economics measured on the availability clock, a one-day floor on time to expiry, and no 0DTE features | 2026-08-20 |
| `artifacts/rp2_block10_inference/inference.json` → `{D,V}.superior_predictive_ability` (D: `lightgbm|B0+B1+B2`, ΔQLIKE +0.01195, SPA p 0.0010) | `artifacts/rp2_v3/gate9-session-inference/inference.json` (D: `gamma_glm|B0+B1`, ΔQLIKE +0.00408, SPA p 0.0010; V: nothing beats its own B0) | one cross-family race over per-origin rows: it confounded the estimator with the information set and used a sample it did not have | 2026-08-20 |
| `artifacts/rp2_block8_ladder/ladder.json` and `artifacts/rp2_block10_inference/inference.json` → every contrast estimate and p-value | `fix/rp2-v3-session-inference` | the estimate averaged over origins, so a busy session weighed more than a quiet one and an early close weighed less than a full day; the interval resampled single days rather than blocks of five sessions | 2026-08-20 |

| `artifacts/rp2_block6_flow/flow_coverage.json` -> `b2_p95_provider_latency_s` (0.122 s) | `results/rp2-v3-rebuild` (0.280 s) | the reported tail was a median across windows of each window's own 95th percentile, which is not a quantile of any population: a window holding one trade weighed as much as one holding ninety-nine, and quantiles do not merge by averaging. It is now read off a histogram of all 580,549,989 trades. The value falling below the reported mean is not the defect and never was: the distribution is heavy-tailed, with 94.3 % of trades at or under 0.28 s and 0.23 % above 100 s, which is what carries the mean to 1.221 s | 2026-08-21 |

| `artifacts/rp2_block10_inference/inference.json` and the RP2-v2 ladder, every contrast estimate and interval | `rp2-v3-20260821-134741` (published, scientific hash `fdce125264082af5`) | rebuilt end to end under the frozen partition with contemporaneous B1, corrected B2 clocks, fold-local preprocessing, one common evaluation mask, LightGBM aligned to QLIKE, and session-level family-matched inference | 2026-08-21 |
| README "Findings at a glance (2026-08-19)", item 2: B2-over-B1 under the Gamma GLM "positive and statistically supported... up to +0.053" | `rp2-v3-20260821-134741` | the rebuilt contrast is ΔB2\|B1 = −0.02549 in development and −0.00222 in validation, the latter with a 95 % interval excluding zero. The sign is reversed, not the magnitude reduced | 2026-08-21 |

| `artifacts/rp2_ext12_level4/level4_and_tensor.json` -> `D.extension_1_level4`, every estimate and interval (deepsets QLIKE 0.22154, delta vs control -0.05747, delta vs lightgbm -0.08691, log-scale RMSE 1.10246) | the same artifact rebuilt on `feat/rp2-v3-level4-sequence` (deepsets QLIKE 0.17017, delta vs control -0.00469 [-0.01967,+0.00729] p 0.6017, delta vs lightgbm -0.03553 [-0.07755,-0.01184] p 0.0010, log-scale RMSE 0.58241) | the masked max-pool filled padded positions with -1e9 and the repair tested for -inf, so 448 of 2,975,222 forward passes carried a feature of magnitude 1e9 into the head; the first epoch recorded MSE 1.7e14 against the control's 43.5. The preregistered conclusion is unchanged - the sequence fails both references - but 92 % of the reported effect was the sentinel | 2026-08-21 |

| `artifacts/rp2_v3/rp2-v3-20260821-134741/rp2_block8_ladder/ladder.json` and `rp2_block10_inference/inference.json` -> every role-D figure, and the six role-D rows of the twelve-contrast table in `docs/rp2_v3/VERDICT.md` | the same producers run against a B0 built from re-acquired bars (`data/fmp/rp2_ohlcv_repair/underlying_1min_repair.parquet`) | two development-only bar stores carried no high, low or volume and the session grid invented them as high == low == close and volume == 0, making three B0 features exactly zero on 22,967 of 152,954 development origins and none of the 31,678 validation ones. B0 improves and the development B1 increment falls by about two fifths - ridge_log +0.00424 to +0.00250, gamma_glm +0.00408 to +0.00234, lightgbm_qlike +0.00381 to +0.00314 - while validation is unchanged to the digit. Decision 86 | 2026-08-21 |
| `docs/rp2_v3/VERDICT.md` -> the claim that `ridge_log` refutes | decision 86 | the corrected development effect +0.00250 is below validation's own minimum detectable effect of 0.00268, so validation was not powered to see it: about 37 sessions would be needed against 32 available. All three families are underpowered rather than one refuting | 2026-08-21 |
| `artifacts/rp2_v3/*/scorecard.json` -> `b1.b1_p95_quote_age_s` (1723.92 s), `b1.b1_median_quote_age_s` (579 s) and `b2.b2_multileg_share` (0.231077) | the same fields read off summed histogram bins and pooled by premium | the first two were a median across origins of each origin's own quantile, which is not a quantile of any population; the third was an unweighted mean of ratios whose denominators span a factor of fourteen, against a pooled share of 0.236861. Decision 87 | 2026-08-21 |

| `artifacts/rp2_ext12_level4/level4_and_tensor.json` -> `D.extension_2_tensor` (delta -0.00007 [-0.00065,+0.00053] p 0.7676) and `D.extension_1_level4` (delta -0.00664 [-0.01392,-0.00138] p 0.0080), both measured on tensors whose inputs hashed `793c56cb033f3b78` | the same artifact rebuilt on tensors hashing `8c6ed992d58a1414` (tensor +0.00044 [-0.00017,+0.00105] p 0.1599; sequence -0.00607 [-0.01249,-0.00130] p 0.0070) | every trade was marked at the close of its own minute, so a trade at 10:15:03 was priced at 10:16:00 and its Greeks, log-moneyness and bucket followed from a price in its own future. The tensor contrast changes sign and grows sixfold while still containing zero; the sequence contrast is unchanged in substance and still excludes it. Decision 89 | 2026-08-21 |

| `artifacts/rp2_v3/rp2-v3-20260821-134741/` in full, and the `docs/rp2_v3/VERDICT.md` built on it -- in particular its claim that `ridge_log` refuted the development effect on adequate power | `rp2-v3-20260822-054000`, scientific hash `07456efcd4bce4ec` at commit `7225fdf` | the development baseline carried three B0 features that were fabricated zeros on 22,967 of 152,954 rows (decision 89). Repaired, the development B1 effect falls from +0.00424 to +0.00250 for ridge_log, +0.00408 to +0.00234 for gamma_glm and +0.00381 to +0.00314 for lightgbm_qlike, while validation is unchanged to the digit. No family is now powered to detect a development-sized effect, so the refutation is withdrawn and every validation null is absence of evidence. Decision 90 | 2026-08-22 |
| `artifacts/rp2_v3/rp2-v3-20260822-054000/` in full, and the six role-D rows of the twelve-contrast table in `docs/rp2_v3/VERDICT.md` built on it -- in particular its finding that `lightgbm_qlike` carried the only positive B2 increment of the twelve to exclude zero | `rp2-v3-20260822-114000`, scientific hash `5c6a4c0e2b661e91` at commit `da2fb7b` | the repaired baseline still counted 2,347 minutes whose provider-recorded volume was zero while that minute's high exceeded its low; 899 were recovered from the second provider and the remaining 1,448 are now carried as missing rather than as a measured zero. Development ΔB2\|B1 for `lightgbm_qlike` falls from +0.00113 [+0.00042, +0.00188] to +0.00046 [−0.00019, +0.00110], which contains zero, so that finding is withdrawn; no B2 increment now excludes zero on the positive side. Development ΔB1 moves to +0.00211, +0.00230 and +0.00327, and 51 forecast fields moved of which every one is a development field and none a validation field. Decision 91 | 2026-08-22 |

| `artifacts/rp2_block7_dml/dml.json` (`01775dcb…`, generated 2026-08-19) and sections 1-5 of `docs/rp2/block7_dml_v1.md` built on it -> the core joint test (D Wald 241.71376, p 2.9915e-46; V 17.58772, p 0.0623294), the jump test (D 14.09691, p 0.168617; V 19.92218, p 0.0299974) and all ten treatment rows | `artifacts/rp2_v3/rp2-v3-20260822-114000/rp2_block7_dml/dml.json` (`030b2e35…`): core D Wald 98.75420, p 9.67324e-17; core V 5.79616, p 0.832088; jump D 18.34069, p 0.0494815 and V 18.76234, p 0.0433873 | the page was written for the 2026-08-19 cascade and never rebuilt, so it went on publishing a panel of 89,889 discovery origins over 230 sessions against the current 152,954 over 389. Every figure in section 2 reproduces to five decimals against the artifact the page cites, so nothing was fabricated there - it is a frozen description of a superseded run. Three of the ten treatments change sign and one keeps its significance while doing so: `b2_5m_trades` moves from theta +0.056656 (t +5.32) to −0.0761583 (t −4.653) in discovery. No treatment now clears 0.05 in both universes, so "only `b2_5m_trades` carries the same sign at conventional significance in both" has nothing left to point at, and validation moves from a near miss (p 0.0623) to no rejection at all (p 0.832). Section 2's paragraph that validation's B0 "carries no market-wide state" preserved a defect decision 75 had already fixed: `validation_market` (`src/mds650/rp2/bars.py:58-62`) supplies SPY and QQQ across the validation sessions, and the current artifact's V nuisance set is the same 32-feature list as D's. Guarded from here on by `tests/contract/test_block7_narrative_matches_artifact.py` | 2026-08-23 |
| `docs/INDEX.md` block 7 row: "**The positive finding.** Double machine learning rejects the null at p = 3e-12; the Hawkes burst-intensity innovation and buyer-initiated premium share replicate across universes" | the current section of `docs/rp2/block7_dml_v1.md`, measured on `rp2-v3-20260822-114000` | the p-value was not fabricated and was not block 7's. Wald 76.0581, p 2.9600e-12 over 89,889 origins and 230 clusters is `artifacts/rp2_ext1_mechanism_utility/mechanism_utility.json` -> `D.a_other_targets.y_rv_30` (generated 2026-08-18), whose `replicated_treatments` field reads exactly `["b2_5m_hawkes_innovation", "b2_5m_buy_premium_share"]` - the two the INDEX row named. It is the pre-rebuild panel, a different target and an extension rather than the block, advertised as the block's current headline. On the current run the core test gives p 9.67324e-17 in discovery and 0.832088 in validation, and no treatment clears 0.05 in both universes with the same sign, so the replication claim has no support. `docs/rp2/VALIDATION_REPORT.md` had already withdrawn it in prose and correctly kept 76.1 / 3e-12 in its before-and-after table; the INDEX had not followed | 2026-08-23 |
| `docs/rp2/block7_dml_v1.md` §1 "38 nuisance features", §4 (D Wald 501.87 p 5.9e-72; V 548.92 p 4.4e-81; full-block jump p 0.031; core jump p 0.224; "the same universe returns p = 0.059 on the ten-treatment core test") and §5 (p 6e-39 in D, p 0.059 in V); and the same figures re-cited in `docs/rp2/block13_execution_map_v1.md` row 7 (501.9 full / 206.8 core / p 6e-39 / p 0.059) | withdrawn, not replaced | none of these reproduces against any artifact in this tree, including the one the page itself cites. That artifact records 49 nuisance features, full-block D Wald 486.71399 p 4.9535e-69, full-block V 576.71477 p 1.60728e-86, full-block jump p 0.0152657, core jump p 0.168617, core D p 2.9915e-46 and core V p 0.0623294 - so the page is internally inconsistent with its own citation, not merely stale. All 375 JSON artifacts under `artifacts/` were scanned for each value; the only figures that turn up elsewhere are §1's origin and session pairs (89,889 / 230 and 31,131 / 80) and the V p 0.05938, all three in `artifacts/rp2_ext1_mechanism_utility/mechanism_utility.json`. The verdict "`H_B2,J` is recorded as not supported" rested on the two withdrawn jump figures, so it is withdrawn with them rather than reversed: what the current run measures is core D p 0.0494815 and core V p 0.0433873, both nominal and neither adjusted for the three outcomes tested. A decision number for this withdrawal is left for the integration pass to assign in `docs/methodology_decisions.md`, because assigning one from a repair branch would collide with any other branch doing the same | 2026-08-23 |

| `artifacts/rp2_v3/rp2-v3-20260822-114000/` in full, the `docs/rp2_v3/VERDICT.md` built on it, and README "Findings at a glance (2026-08-22)" — in particular the statements that no B2 increment excluded zero on the positive side, that `lightgbm_qlike` was the one family positive in both roles, and the DML headline p 9.67324e-17 with Wald 98.75 | `rp2-v3-20260824-remeasure`, scientific hash `b3b9f03625048f20` at commit `9df1b107` | the run was measured with code the repository afterwards documented as defective in six places: a rolling window sum that let one absent minute void every later origin in its session-asset (non-finite `volume_30` origins in D fall from 7,094 to 1,522 of 152,954), a log transform that turned an exact zero into −27.631 with no missing indicator (`jump_back_30` falls from 60.91 % of the training sum of squares to 0.01 %), a recalibration that undid the smearing correction it sat on, a boosting round count that was a module constant, and two block-7 reporting contracts. Remeasured, `gamma_glm`'s development ΔB2\|B1 moves from −0.00374 to −0.00002 and `ridge_log`'s from −0.01138 to +0.00017. They were initially labelled formally equivalent within ±0.00137; that label is withdrawn because the implementation checked interval compatibility with a margin, not TOST. `lightgbm_qlike` moves from +0.00046 (contains zero) to +0.00108 (p 0.0010 under the exploratory contract; it does not replicate: V p 0.0470, Giacomini–White p 0.3544). Its development ΔB1 falls from +0.00327 to +0.00123 and its validation ΔB1 from +0.00092 to −0.00383, ending the statement that it was positive in both roles. Unlike the bar-store repair, this one touches both roles, and validation moved: 113 of 162 comparable scorecard fields differ between the runs. The DML core joint test moves to p 2.667e-22, Wald 131.81 in D and to p 0.832 in V | 2026-08-24 |
| `artifacts/rp2_v3/rp2-v3-20260824-remeasure/` and its current-claim interpretation in `docs/rp2_v3/VERDICT.md` | invalidated pending a new rebuild; no replacement run exists | Block 8 selected boosted-model rounds using session-aware inner validation while Block 10 did not, so the ladder and inference did not execute the same model protocol. PIT v2.2 also records `SAFE_TO_RECONCILE_EXISTING_RESULTS=NO`. The frozen files remain as historical measurements, but no result from this run is currently eligible | 2026-08-26 |
| `docs/gate4_prospective_design_v1.md`, `reports/gate_cascade_report_20260817.md`, `reports/defense_deck_v2.md`, and `reports/proposal_draft_v2.md` | `data/CANONICAL_STATE.json`, `docs/phase8_bridge_protocol_v2.md`, and `reports/final_report_draft_v2.md` | These drafts promoted the invalidated −0.0277/year decay line, treated Phase 8 as confirmatory/TOST-armed, and mixed internal execution narration with the researcher-facing surface. They are removed from the root-only public release; this register preserves why. Any historical wording here that called a result “formally equivalent” is also withdrawn: the implementation checked interval compatibility with a margin, not TOST | 2026-08-28 |
| `docs/canonical_claims_and_limitations.md` and `docs/results_reconciliation_v2.md` → their rival v2 numerical-authority claims, including ledger `CLM-016` (+0.03396090) | `data/CANONICAL_STATE.json` and its generated `STATUS.md` | The ledger and decision 53's reconciliation source asserted separate authority. Decision 63 superseded both for current eligibility. The RP2-v3 ΔB2\|B1 value (−0.00002) uses a different model ladder, partition and estimand; it is not a one-for-one replacement. Both measurements are historical, and neither is a current eligible headline while `PIT_V22_RECONCILIATION_BLOCKED` remains active | 2026-08-30 |

Later rows are added by the rebuild gate (`results/rp2-v3-rebuild`) once the RP2-v3 run
exists and its `run_id` is known. A gate that supersedes a number before that point records
it here against its own branch, because a reader of the frozen artifact would otherwise
find the old value with nothing saying it has been replaced.

## Rule

- No frozen artifact under `artifacts/` is overwritten, moved or removed by an RP2-v3 gate.
- A superseding run writes under `artifacts/rp2_v3/<run_id>/` only.
- A claim withdrawn rather than replaced is recorded in `docs/methodology_decisions.md`
  with its withdrawal reason, and is also listed here.

## Corrected-protocol replacement — 2026-08-27

| Superseded artifact or claim | Replacement | Reason | Date |
| --- | --- | --- | --- |
| `artifacts/rp2_v3/rp2-v3-20260824-remeasure/` and its Block 8/10 interpretation | `rp2-v3-20260827-remediation3`, scientific hash `386610a4908d601c…` at commit `e7728ebb` | The new run harmonizes session-aware boosting-round selection between the ladder and inference and verifies identical forecasts, losses and rounds for all three primary families. The corrected run completed 13/13 steps, but remains historical because `PIT_V22_RECONCILIATION_BLOCKED` still forbids a current claim | 2026-08-27 |

## Timing-and-role replacement — 2026-08-31

| Superseded artifact or claim | Replacement | Reason | Date |
| --- | --- | --- | --- |
| `artifacts/rp2_v3/rp2-v3-20260827-remediation3/` and its twelve-contrast interpretation | `rp2-v3-20260831-timing-role-remediation`, scientific hash `7d544aa334b70ce8…` at commit `cbdd0b58` | Three result-changing defects remained: source roles were trusted rather than derived by date, B0 observed the underlying 180 seconds later than the 120-second B1/B2 cutoff, and B1 hardcoded a 16:00 ET expiry close instead of XNYS. The replacement completed 13/13 steps with `--forbid-sealed-cohorts`; PIT v2.2 still blocks a current claim | 2026-08-31 |
| `artifacts/rp2_ext1_directional_factorial_v1/results.json` and the factorial table in `docs/rp2/extension_b2_directional_utility_v2.md` | `artifacts/rp2_ext1_directional_factorial_v2/results.json` and the current table in `docs/rp2_v3/VERDICT.md` | The factorial nuisance block uses B0+B1, so the clock repairs require remeasurement. The new result preserves the exact 10/12 treatment widths, same masks within coverage cells and `sealed_cohorts_read=0`; its registered attribution remains `TREATMENT_SET`, while the frozen-to-current V decline remains unexplained by either coverage or the 12-feature set | 2026-08-31 |
